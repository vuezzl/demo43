```
授课资料网盘地址：

```

#### Git讲解的内容

```
0. 重要提示：如何理解我们对Git做的事情？
	我们对Git的操作，就类似于对网盘的操作。
	先在自己电脑里准备资料，然后备份并记录下，再上传一份到网盘备份。
1. Git介绍
	Git的作用(为什么用Git?)
        方便多人协同开发
        方便代码版本控制和备份
    Git的特点：
    	分布式版本控制
    	版本控制在.git文件目录中完成
    Git操作流程
    	创建远程仓库--->克隆远程仓库到本地--->编写代码--->建立跟踪、备份和记录--->上传到远程仓库 
2. 工作区、版本库<.git>（暂存区和仓库区）
	这三个区，在代码管理的过程中分工明确，作用各不相同，是Git的作者设计并实现好的
	就跟生产面包的流水线一样的，每个环节处理什么，都是设计好的。
	工作区：存放有代码的文件目录
	版本库：提供了暂存区、默认的master分支和指向master分支第一个版本的HEAD指针
		暂存区：建立跟踪
		仓库区：将代码备份到master分支，并用指针做个标记，然后加个描述。(类似记流水账和自己备份文件)
3. 需要掌握的命令
	<git init>：
		用于创建本地仓库的。但是，工作中不用，因为工作时都是clone远程的仓库。所以该命令自己玩玩儿即可
	
	<git clone>：
		用于下载远程仓库的，进公司第一天会执行的，后期如果删除了本地仓库想重新下载远程仓库时也会执行的
	
	<git add>：
		用于对代码建立跟踪的，效果是将代码由工作区添加到暂存区
		<git add .>：一次性的将所有的新文件或者修改文件添加到暂存区
		<git add 文件名>：将指定的新文件或者修改文件添加到暂存区
	
	<git commit -m "版本描述">：
		对代码进行备份，并记录版本和描述信息的。默认会将代码备份到master分支
	
	<git status>：
		查看代码的状态，用于检查命令执行后的结果，或者引导下一步操作的
		
	<git push>：
		将本地备份的代码和版本记录上传到远程仓库 (类似于将文件上传到网盘)
		注意：
			push前一定要先pull，不然容易出现代码冲突。
			而且每次push还需要设置密码，但是可以通过配置，达到保存密码的效果
			如果在每次 push 都需要设置账号与密码，那么可以设置记住密码
			长期存储密码：
				git config --global credential.helper store
	
    <git pull>：
    	将远程仓库中的新代码同步到本地仓库
    	需要跟你<git clone>区分开
    	<git clone> 用于下载整个远程仓库，当没有本地仓库时才需要使用的
        <git pull> 用于从远程仓库同步本地仓库没有的代码，pull命令的基础必须先有仓库

	<git log> or <git reflog>：
		查看版本控制的日志
	
	<git reset --hard 版本号>：
		用于回退版本的，不会轻易使用的，只有当自己的代码出现了惊天大BUG，需要重写时才会尝试回退版本
		
5. 单人对代码的管理命令
	git clone 远程仓库地址
	编辑代码
	git add .
	git commit -m "版本描述"
	git push
```

#### 测试

```
1. 以下关于Git的描述正确的是：（多选）
A：Git是一个用于管理源代码的工具。
B：Git的目的就是为了方便多人协同开发并方便管理源代码。
C：Git无法实现分布式版本控制。
D：Git的版本控制只能在本地完成。
```

```
2. 以下关于Git的工作区、暂存区和仓库区描述正确的是：（多选）
A：Git工作区的代码就是编辑器中编辑的代码。
B：可以给Git暂存区的代码进行版本记录。
C：可以给Git仓库区的代码进行版本记录。
D：可以直接将暂存区的代码推送到远程仓库。
```

```
3. 以下关于Git的命令中是用来创建本地仓库的是：（单选）
A：git add。
B：git commit。
C：git init。
D：git config。
```

```
4. 以下关于Git的命令中描述正确的是：（多选）
A：git add：将代码由暂存区添加到工作区。
B：git commit：对仓库区的代码进行备份和版本记录。
C：git status：查看当前仓库的状态。
D：git reset：重置版本的记录。
```

```
5. 以下关于Git操作远程仓库命令中描述正确的是：（多选）
A：git clone：将远程仓库代码克隆到本地。
B：git pull：从远程仓库拉取最新的版本代码。
C：git push：将仓库区的代码推送到远程仓库。
D：git clone和git pull功能相同。
```

```
6. 在性能方面 , 页面上有 1W 个标签 , 需要进行显示和隐藏:
A.v-if 最佳
B.v-show 最佳
```



#### 补充

```
记住密码
	如果在每次 push 都需要设置账号与密码，那么可以设置记住密码
	长期存储密码：git config --global credential.helper store

空文件目录Git默认不会管理
	如果要管理空文件目录，可以在空文件目录中新建 .gitkeep 这个隐藏文件即可
```

#### 进入框架和项目前的忠告

```
大胆去尝试去动手去测试心里的疑惑。
大胆去阅读源代码，python的源代码，Django的源代码，读懂源代码谁都不用求。
大胆去百度，会百度不可耻，不会百度才可耻。
```

